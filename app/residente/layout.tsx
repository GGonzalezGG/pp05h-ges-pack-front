// app/admin/layout.tsx
export const metadata = {
  title: "Retira tu paquete! 📦",
};

export default function residenteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}